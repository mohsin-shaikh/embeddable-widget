# Normalize text to LF in the repo and working tree (matches oxfmt's default endOfLine).
* text=auto eol=lf

# Binary assets — never touch line endings
*.woff2 binary
*.woff binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.pdf binary
